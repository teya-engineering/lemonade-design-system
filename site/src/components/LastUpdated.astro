---
/**
 * Renders nothing on purpose.
 *
 * Starlight puts the last-updated date in the page footer; Lemonade shows it
 * under the page title instead, from `PageTitle.astro`. Overriding this with
 * an empty component keeps the date from appearing twice.
 */
---
